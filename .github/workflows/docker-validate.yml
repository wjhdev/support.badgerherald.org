name: Validate
on:
  push:
    branches:
      - '*'
      - '!master'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Compile webpress & validate docker build
        uses: broadsheet-technology/webpress-compile-build-and-push@test
        with:
          context: .
          push: false
          name: ghcr.io/wjhdev/support-the-herald
